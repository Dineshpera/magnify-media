import {work} from '@/lib/data';import Link from 'next/link';export default function Page(){return <main className="container section"><p className="eyebrow">Portfolio</p><h1 className="h1">A masonry-inspired gallery of momentum.</h1><div className="grid-auto">{work.map((w,i)=><Link className="glass card" style={{minHeight:220+i*25}} href={`/portfolio/${w.slug}`} key={w.slug}><p className="eyebrow">{w.cat}</p><h2>{w.title}</h2><p>{w.metric}</p><p className="muted">Filter-ready, lightbox-ready project system with lazy media slots.</p></Link>)}</div></main>}
