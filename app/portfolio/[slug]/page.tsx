import {work} from '@/lib/data';import {notFound} from 'next/navigation';export function generateStaticParams(){return work.map(({slug})=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const w=work.find(x=>x.slug===(await params).slug);if(!w)return notFound();return <main className="container section"><p className="eyebrow">Portfolio / {w.cat}</p><h1 className="h1">{w.title}</h1><div className="glass card"><h2>{w.metric}</h2><p className="muted">Animated preview, media gallery, video embeds, and lightbox integration point.</p></div></main>}
