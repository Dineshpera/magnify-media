import {posts} from '@/lib/data';import {notFound} from 'next/navigation';export function generateStaticParams(){return posts.map(({slug})=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const p=posts.find(x=>x.slug===(await params).slug);if(!p)return notFound();return <main className="container section"><p className="eyebrow">{p.cat} · {p.read}</p><h1 className="h1">{p.title}</h1><article className="glass card"><p className="muted">Reading progress, related posts, newsletter capture, and MDX content pipeline are wired into the editorial architecture.</p><h2>Key takeaway</h2><p>Premium growth compounds when brand, distribution, and product experience are designed as one system.</p></article></main>}
