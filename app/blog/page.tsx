import {posts} from '@/lib/data';import Link from 'next/link';export default function Page(){return <main className="container section"><p className="eyebrow">Blog</p><h1 className="h1">Ideas for durable growth.</h1><div className="grid-auto">{posts.map(p=><Link className="glass card" href={`/blog/${p.slug}`} key={p.slug}><p className="eyebrow">{p.cat} · {p.read}</p><h2>{p.title}</h2><p className="muted">MDX-ready editorial template with SEO metadata, related posts, newsletter, and reading progress.</p></Link>)}</div></main>}
