import type {MetadataRoute} from 'next';import {services,work,posts} from '@/lib/data';export default function sitemap():MetadataRoute.Sitemap{const base='https://magnifymedia.example';const staticRoutes=['','about','services','portfolio','case-studies','industries','pricing','blog','careers','contact','privacy','terms','search'];return [...staticRoutes.map(r=>({url:`${base}/${r}`,lastModified:new Date()})),...services.map(([s])=>({url:`${base}/services/${s}`,lastModified:new Date()})),...work.map(w=>({url:`${base}/portfolio/${w.slug}`,lastModified:new Date()})),...work.map(w=>({url:`${base}/case-studies/${w.slug}`,lastModified:new Date()})),...posts.map(p=>({url:`${base}/blog/${p.slug}`,lastModified:new Date()}))]}
