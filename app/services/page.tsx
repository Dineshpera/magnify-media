import {services} from '@/lib/data';import Link from 'next/link';export default function Page(){return <main className="container section"><p className="eyebrow">Services</p><h1 className="h1">Senior growth capabilities.</h1><div className="grid-auto">{services.map(([slug,n,d,Icon])=><Link className="glass card" href={`/services/${slug}`} key={slug}><Icon/><h2>{n}</h2><p className="muted">{d}</p></Link>)}</div></main>}
