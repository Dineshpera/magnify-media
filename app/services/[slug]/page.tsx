import {services} from '@/lib/data';import {notFound} from 'next/navigation';export function generateStaticParams(){return services.map(([slug])=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x[0]===slug);if(!s)return notFound();const[,n,d,Icon]=s;return <main className="container section"><p className="eyebrow">Service detail</p><h1 className="h1">{n}</h1><Icon size={48}/><p className="muted" style={{fontSize:24}}>{d}</p><div className="grid-auto">{['Audit','Strategy','Production','Optimization'].map(x=><div className="glass card" key={x}><h2>{x}</h2><p className="muted">A senior-led delivery lane with milestones, analytics, and conversion review.</p></div>)}</div></main>}
