import {work} from '@/lib/data';import Link from 'next/link';export default function Page(){return <main className="container section"><p className="eyebrow">Case studies</p><h1 className="h1">Proof, not promises.</h1><div className="grid-auto">{work.map(w=><Link className="glass card" href={`/case-studies/${w.slug}`} key={w.slug}><h2>{w.title}</h2><p>{w.metric}</p></Link>)}</div></main>}
