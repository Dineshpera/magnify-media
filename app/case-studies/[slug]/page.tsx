import {work} from '@/lib/data';import {notFound} from 'next/navigation';export function generateStaticParams(){return work.map(({slug})=>({slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const w=work.find(x=>x.slug===(await params).slug);if(!w)return notFound();return <main className="container section"><p className="eyebrow">Case study</p><h1 className="h1">{w.title}</h1><div className="grid-auto">{['Challenge','Strategy','Execution','Results','KPIs','Timeline','Client quote','Gallery'].map(x=><section className="glass card" key={x}><h2>{x}</h2><p className="muted">{w.metric} through focused creative, media, and web systems.</p></section>)}</div></main>}
