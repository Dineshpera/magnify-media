'use client';
import { useEffect, useRef } from 'react';import * as THREE from 'three';
export default function ThreeHero(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const el=ref.current;if(!el)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(45,1,0.1,100);camera.position.z=5;const renderer=new THREE.WebGLRenderer({antialias:true,alpha:true});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));el.appendChild(renderer.domElement);const geo=new THREE.TorusKnotGeometry(1.25,.28,180,16);const mat=new THREE.MeshStandardMaterial({color:'#7c6dff',roughness:.32,metalness:.65});const mesh=new THREE.Mesh(geo,mat);scene.add(mesh);scene.add(new THREE.AmbientLight('#ffffff',1.2));const light=new THREE.PointLight('#ffffff',6);light.position.set(4,3,4);scene.add(light);let id=0;const resize=()=>{const r=el.getBoundingClientRect();renderer.setSize(r.width,r.height,false);camera.aspect=r.width/r.height;camera.updateProjectionMatrix()};resize();addEventListener('resize',resize);const tick=()=>{mesh.rotation.x+=.004;mesh.rotation.y+=.006;renderer.render(scene,camera);id=requestAnimationFrame(tick)};tick();return()=>{cancelAnimationFrame(id);removeEventListener('resize',resize);renderer.dispose();geo.dispose();mat.dispose();el.innerHTML=''}},[]);return <div ref={ref} style={{width:'100%',height:'100%'}}/>}
