'use client';
import Link from 'next/link';import { useEffect } from 'react';import Lenis from 'lenis';import { ArrowUpRight, Moon, Sun } from 'lucide-react';import { ThemeProvider,useTheme } from 'next-themes';import { Magnetic } from './motion';
export function Providers({children}:{children:React.ReactNode}){useEffect(()=>{const l=new Lenis({lerp:.08});let id=0;const raf=(t:number)=>{l.raf(t);id=requestAnimationFrame(raf)};id=requestAnimationFrame(raf);return()=>cancelAnimationFrame(id)},[]);return <ThemeProvider attribute="class" defaultTheme="dark" enableSystem>{children}</ThemeProvider>}
export function Nav(){const links=['Services','Portfolio','Case Studies','Pricing','Blog','Contact'];return <header className="container" style={{position:'sticky',top:16,zIndex:20}}><nav className="glass" style={{borderRadius:999,padding:10,display:'flex',alignItems:'center',justifyContent:'space-between'}}><Link href="/" style={{fontWeight:900,padding:'0 14px'}}>Magnify Media</Link><div style={{display:'flex',gap:4,alignItems:'center',flexWrap:'wrap'}}>{links.map(l=><Link key={l} href={'/'+l.toLowerCase().replaceAll(' ','-')} className="btn" style={{padding:'.65rem .8rem'}}>{l}</Link>)}<ThemeToggle/><Magnetic><Link href="/contact" className="btn primary">Start <ArrowUpRight size={16}/></Link></Magnetic></div></nav></header>}
function ThemeToggle(){const{theme,setTheme}=useTheme();return <button aria-label="Toggle theme" className="btn" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?<Sun size={16}/>:<Moon size={16}/>}</button>}
export function Footer(){return <footer className="section"><div className="container glass card"><div className="grid-auto"><div><h2 className="h2">Let’s magnify what matters.</h2><p className="muted">Premium strategy, creative, media, and engineering for ambitious brands.</p></div><div><b>Explore</b><p className="muted">Services · Industries · Careers · Search</p></div><div><b>Legal</b><p className="muted">Privacy · Terms</p></div></div></div></footer>}
